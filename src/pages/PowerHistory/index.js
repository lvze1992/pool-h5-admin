import React from 'react';
import { useHistory } from 'react-router-dom';
import './style.scss';
export default function PowerHistory() {
  const history = useHistory();
  return <div className="powerHistory-page">PowerHistory</div>;
}
